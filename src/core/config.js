/**
 * Shared runtime configuration derived from the current browser context.
 * Supports both tahvel.edu.ee and tahveltp.edu.ee origins automatically.
 */
export const TAHVEL_API_URL = window.location.origin + '/hois_back';
